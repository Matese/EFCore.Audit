version: '3.4'

services:
  mssql:
    build:
      context: Docker/
    container_name: mssql
    ports:
      - 1433:1433
    restart: always
    environment:
      - MSSQL_PID=Express
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=P@ssw0rd123 # Note: make sure that your password matches what is in the db-init script 